import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1788344443000}`),a={name:`guide/configuration.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h1><p>Every setting is an environment variable. The full table is in the <a href="/reference/environment">environment reference</a>; this page is about the choices behind them.</p><h2 id="the-credential" tabindex="-1">The credential <a class="header-anchor" href="#the-credential" aria-label="Permalink to “The credential”">​</a></h2><p>Three kinds, tried in this order — explicit beats ambient:</p><ol><li><code>GSC_SERVICE_ACCOUNT_KEY</code> or <code>GSC_SERVICE_ACCOUNT_KEY_FILE</code></li><li><code>GSC_CLIENT_ID</code> + <code>GSC_CLIENT_SECRET</code> + <code>GSC_REFRESH_TOKEN</code></li><li><code>GOOGLE_APPLICATION_CREDENTIALS</code>, or a <code>gcloud</code> login</li></ol><p>A <strong>partial</strong> OAuth triple is a startup error rather than a reason to fall through to the third. That fallback is the dangerous one: a typo in <code>GSC_CLIENT_ID</code> would otherwise leave the server quietly acting as whatever account the machine is logged into, which on a developer workstation is usually someone with access to everything.</p><p><code>GSC_SERVICE_ACCOUNT_KEY</code> accepts raw JSON or base64-encoded JSON. Both spellings are needed in practice — a key pasted into a shell profile keeps its braces and newlines, while anything that travels through a CI secret store or a Kubernetes secret arrives encoded.</p><p>If you paste an OAuth <em>client secrets</em> file where the service account key belongs — they are both JSON from the same console page — the server says so by name rather than failing later inside the signing code.</p><h2 id="the-default-property" tabindex="-1">The default property <a class="header-anchor" href="#the-default-property" aria-label="Permalink to “The default property”">​</a></h2><p><code>GSC_SITE_URL</code> makes <code>site_url</code> optional on every tool that takes one. That is not only convenience: a property the model never spells out is a property it cannot misspell into a different one, and the two spellings for a site hold different data.</p><p><code>add_site</code> is the one exception and deliberately does <strong>not</strong> use the default. The default names the property you already work with; that argument names one that does not exist yet, and silently re-adding the default is never what was meant.</p><h2 id="restricting-which-properties-are-reachable" tabindex="-1">Restricting which properties are reachable <a class="header-anchor" href="#restricting-which-properties-are-reachable" aria-label="Permalink to “Restricting which properties are reachable”">​</a></h2><p><code>GSC_ALLOWED_SITES</code> takes a comma-separated list, and anything not in it is refused before a request goes out. It exists for the case where one Google account can see properties belonging to different parts of your life — work and personal, or several clients.</p><p>Most tools name a property, and for those the check sits in <code>resolveSite</code>. Two shapes of tool do not name one, and each is checked separately rather than being exempt:</p><ul><li>the <strong>Indexing API</strong> tools take a page URL. It is matched against the list the way Search Console scopes a property: <code>sc-domain:example.com</code> covers every subdomain, and <code>https://example.com/shop</code> covers <code>…/shop/item</code> but not <code>…/shopping</code>.</li><li>the <strong>verification</strong> tools take an opaque resource id, which says nothing about the property it belongs to. The resource is fetched first and its own site block mapped back to a property.</li></ul><p><code>list_sites</code> and <code>list_verified_sites</code> also filter to the allowlist and report how many entries they withheld — a property listed but refused by every other tool is a name disclosed for nothing, and the ids from <code>list_verified_sites</code> are exactly what <code>unverify_site</code> acts on.</p><h2 id="read-only-mode" tabindex="-1">Read-only mode <a class="header-anchor" href="#read-only-mode" aria-label="Permalink to “Read-only mode”">​</a></h2><p><code>GSC_READ_ONLY=true</code> does two things, and the second is the one that matters:</p><ul><li>the nine write tools are never registered, so they are not in <code>tools/list</code> and a call to one is answered exactly as an unknown name would be;</li><li>the server requests the <code>webmasters.readonly</code> scope instead of <code>webmasters</code>, so a write is impossible below the tool layer rather than only above it.</li></ul><p><code>1</code> and <code>yes</code> are read the same way as <code>true</code>, in any casing or padding — those are the spellings a compose file or a systemd unit reaches for, and a protection that fails off on the wrong word is worse than one that is a little tolerant. <code>ELICITATION</code> is the deliberate opposite: it is a permission, so only the exact word grants it.</p><p>It does not narrow the Site Verification scope, because Google does not offer a read-only one — <code>siteverification.verify_only</code> is write-only and cannot list. If that matters, deny the verification tools as well and the scope goes away with them.</p><h2 id="turning-the-approval-dialog-off" tabindex="-1">Turning the approval dialog off <a class="header-anchor" href="#turning-the-approval-dialog-off" aria-label="Permalink to “Turning the approval dialog off”">​</a></h2><p><code>delete_site</code>, <code>delete_sitemap</code>, <code>unverify_site</code> and <code>update_site_owners</code> ask a person through MCP elicitation before they act. <code>ELICITATION=false</code> takes them to the two-call token instead. It does not remove the guard; there is no setting in which a guarded call goes unannounced.</p><p>The variable deliberately carries no <code>GSC_</code> prefix, which means it reaches every MCP server in the same environment, and — unlike <code>GSC_READ_ONLY</code> — a value it does not recognise <strong>stops the server</strong> rather than failing off. See <a href="/guide/approval">Asking a person</a>.</p><h2 id="choosing-the-tools-that-load" tabindex="-1">Choosing the tools that load <a class="header-anchor" href="#choosing-the-tools-that-load" aria-label="Permalink to “Choosing the tools that load”">​</a></h2><p>Twenty-one tools is a lot to put in front of a model that needs three of them. Every visible tool costs context on every request, and a model picks the right one far more reliably from a handful.</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">GSC_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">essential</span><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;">            # the curated five</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">GSC_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&#39;list_*,get_site&#39;</span><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;">    # exact names, or a prefix with one *</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">GSC_DENY_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&#39;delete_*&#39;</span><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;">            # subtracted from whatever allow left</span></span></code></pre></div><p>Allow decides what is in; deny is subtracted from it. A pattern is a literal prefix plus exactly one trailing <code>*</code> — <code>*_sites</code> and <code>list_*_x</code> are rejected outright, because they look plausible, match nothing, and would otherwise be silent forever.</p><p><code>essential</code> is <code>list_sites</code>, <code>get_site</code>, <code>list_sitemaps</code>, <code>query_search_analytics</code> and <code>inspect_url</code>. All five read; it is the preset for letting a model look at a property without being able to change it.</p><p><strong>An entry that matches no tool stops the server</strong>, naming the entry and listing the real names. An ignored typo would leave a tool missing from <code>tools/list</code> with nothing pointing at the cause, and nobody traces an absence back to an environment variable.</p><p>Naming a write tool while <code>GSC_READ_ONLY</code> is set is also an error, and says so — &quot;unknown tool&quot; would be the one answer that is wrong, since the tool exists and read-only is why it is not there.</p><h3 id="it-narrows-the-credential-too" tabindex="-1">It narrows the credential too <a class="header-anchor" href="#it-narrows-the-credential-too" aria-label="Permalink to “It narrows the credential too”">​</a></h3><p>The OAuth scopes this server requests are derived from the tools that are actually registered, not hard-coded. A server denied the Indexing tools never asks Google for the Indexing scope.</p><p>That is more than tidiness. For a service account under domain-wide delegation the scopes are allowlisted by a Workspace administrator, and asking for one that was not delegated fails the <strong>whole</strong> token request with <code>unauthorized_client</code> — so a server that always asked for all three would be unusable for someone granted only Search Console.</p>`,34)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};